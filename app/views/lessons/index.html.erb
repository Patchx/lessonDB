<p id="notice"><%= notice %></p>

<h1>Listing Lessons</h1>

<table>
  <thead>
    <tr>
      <th>Title</th>
      <th>Grade</th>
      <th>Subject</th>
      <th>State</th>
      <th>Ese</th>
      <th>Standards</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @lessons.each do |lesson| %>
      <tr>
        <td><%= lesson.title %></td>
        <td><%= lesson.grade %></td>
        <td><%= lesson.subject %></td>
        <td><%= lesson.state %></td>
        <td><%= lesson.ese %></td>
        <td><%= lesson.standards %></td>
        <td><%= link_to 'View Lesson', lesson %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Lesson', new_lesson_path %>
